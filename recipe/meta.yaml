{% set name = "s3fs" %}
{% set version = "2026.9.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://files.pythonhosted.org/packages/source/{{ name[0] }}/{{ name }}/s3fs-{{ version }}.tar.gz
  sha256: ec078ff0122d28fd21d36ea4987794e4f42ecd6e8785eeaa083208771c1464e5

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python {{ python_min }}
    - setuptools
  run:
    - aiobotocore >=2.19.0,<4.0.0
    - aiohttp
    - fsspec >=2026.9.0,<2026.9.1
    - python >={{ python_min }}

test:
  imports:
    - s3fs
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: http://s3fs.readthedocs.io/en/latest/
  summary: Convenient Filesystem interface over S3
  license: BSD-3-Clause
  license_file: LICENSE.txt
  dev_url: https://github.com/dask/s3fs
  doc_url: https://s3fs.readthedocs.io/en/latest/

extra:
  recipe-maintainers:
    - martindurant
    - mrocklin
    - tomaugspurger
